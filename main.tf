# Composition root. Service modules are connected in the challenge-named files.
# Production defaults are never altered for the emulator; scripts/local.py
# creates an isolated working copy with explicit local provider overrides.
